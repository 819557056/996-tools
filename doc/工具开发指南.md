# 工具开发指南

本文档详细说明如何在 996-Tools 项目中创建新的工具功能。

## 📋 创建新工具流程概览

创建一个新工具需要修改以下 **4 个部分**：

| 步骤 | 文件路径 | 说明 |
|------|----------|------|
| 1 | `src/views/tools/<ToolName>/` | 工具目录，包含视图组件和逻辑代码 |
| 2 | `src/lib/tools-data.ts` | 工具元数据配置 |
| 3 | `src/router/index.ts` | 路由配置 |
| 4 | `locales/zh.yml` 和 `locales/en.yml` | 国际化文本 |

## 🚀 快速创建（推荐）

使用自动化脚本一键创建工具骨架：

```bash
pnpm create-tool <tool-path-name> [options]
```

### 选项说明

| 选项 | 简写 | 说明 |
|------|------|------|
| `--key` | `-k` | 工具标识键 |
| `--name` | `-n` | 工具名称（中文） |
| `--desc` | `-d` | 工具描述 |
| `--category` | `-c` | 工具分类（一级菜单） |
| `--new-category` | - | 创建新的一级菜单分类 |
| `--icon` | `-i` | 图标名称 |
| `--yes` | `-y` | 使用默认值，跳过确认 |

### 示例

```bash
# 交互式创建（推荐新手）
pnpm create-tool timestamp-converter

# 指定分类（放到"文本处理"菜单下）
pnpm create-tool timestamp-converter -c Text_Manipulation -y

# 创建工具并放到新的一级菜单下
pnpm create-tool date-picker --new-category "Date_Tools" -y

# 完整参数创建
pnpm create-tool timestamp-converter -k timestamp -n "时间戳转换器" -d "Unix时间戳与日期互转" -c Text_Manipulation -i Clock -y
```

执行后只需要专注于编写 `src/views/tools/<ToolName>/use<ToolName>.ts` 中的具体实现逻辑。

---

## 📁 菜单分类（一级菜单）说明

工具在侧边栏的位置由 `category` 字段决定。

### 现有分类

| 分类标识 | 中文名称 | 英文名称 |
|----------|----------|----------|
| `Encryption_Tools` | 加密工具 | Encryption Tools |
| `Security_Tools` | 安全工具 | Security Tools |
| `Text_Manipulation` | 文本处理 | Text Manipulation |
| `Image_Videos` | Image & Videos | Image & Videos |
| `ID_Generators` | ID 生成器 | ID Generators |
| `Code_Tools` | 代码工具 | Code Tools |

### 创建新的一级菜单

**方式一：交互式创建**

运行 `pnpm create-tool xxx` 时，在分类选择步骤选择"创建新分类"。

**方式二：命令行参数**

```bash
pnpm create-tool my-tool --new-category "Date_Tools" -y
```

**方式三：手动创建**

1. 在 `locales/zh.yml` 的 `sidebar.categories` 下添加：
   ```yaml
   sidebar:
     categories:
       Date_Tools: '日期工具'
   ```

2. 在 `locales/en.yml` 的 `sidebar.categories` 下添加：
   ```yaml
   sidebar:
     categories:
       Date_Tools: 'Date Tools'
   ```

3. 在工具的 `category` 字段使用新分类名：
   ```typescript
   {
     category: "Date_Tools",
     ...
   }
   ```

---

## 📝 手动创建详细步骤

### 步骤 1：创建工具目录及文件

在 `src/views/tools/` 目录下创建以 PascalCase 命名的工具目录。

例如：`src/views/tools/TimestampConverter/`

在该目录下创建两个文件：
1. `index.vue` - 界面组件
2. `useTimestampConverter.ts` - 逻辑与状态管理

#### 1.1 逻辑文件 (`useTimestampConverter.ts`)

```typescript
import { ref } from 'vue'
import { useI18n } from 'vue-i18n'
import { useToast } from '@/composables/useToast'

export function useTimestampConverter() {
  const { t } = useI18n()
  const { toast } = useToast()

  // 状态
  const input = ref('')
  const output = ref('')

  // 逻辑
  function handleAction() {
    // ... 实现逻辑
  }

  return {
    t,
    input,
    output,
    handleAction
  }
}
```

#### 1.2 界面文件 (`index.vue`)

```vue
<template>
  <div class="max-w-4xl">
    <BaseCard class="p-6">
      <!-- 标题区域 -->
      <div class="mb-6">
        <h1 class="text-2xl font-bold mb-2">{{ t('tools.timestamp.title') }}</h1>
        <p class="text-sm text-muted-foreground">
          {{ t('tools.timestamp.description') }}
        </p>
      </div>

      <!-- 内容区域 -->
      <div class="space-y-6">
        <!-- 绑定来自 useTimestampConverter 的状态 -->
        <BaseTextarea v-model="input" />
        <BaseButton @click="handleAction">转换</BaseButton>
      </div>
    </BaseCard>
  </div>
</template>

<script setup lang="ts">
import BaseCard from '@/components/ui/BaseCard.vue'
import BaseButton from '@/components/ui/BaseButton.vue'
import BaseTextarea from '@/components/ui/BaseTextarea.vue'
import { useTimestampConverter } from './useTimestampConverter'

// 使用组合式函数
const { t, input, handleAction } = useTimestampConverter()
</script>
```

#### 可用的 UI 组件

| 组件 | 路径 | 用途 |
|------|------|------|
| `BaseCard` | `@/components/ui/BaseCard.vue` | 卡片容器 |
| `BaseButton` | `@/components/ui/BaseButton.vue` | 按钮 |
| `BaseInput` | `@/components/ui/BaseInput.vue` | 输入框 |
| `BaseTextarea` | `@/components/ui/BaseTextarea.vue` | 多行文本框 |
| `BaseLabel` | `@/components/ui/BaseLabel.vue` | 标签 |
| `BaseSelect` | `@/components/ui/BaseSelect.vue` | 下拉选择 |
| `Badge` | `@/components/ui/Badge.vue` | 徽章 |

#### 常用图标

项目使用 `lucide-vue-next` 图标库：

```typescript
import { 
  Clipboard,     // 复制
  RefreshCw,     // 刷新
  Download,      // 下载
  Upload,        // 上传
  Check,         // 成功
  X,             // 关闭/错误
  Settings,      // 设置
  Lock,          // 安全/密码
  Text,          // 文本
  Hash,          // 哈希
  Link,          // 链接
  QrCode,        // 二维码
  IdCard,        // ID
  FileJson,      // JSON
  Binary,        // 二进制
} from 'lucide-vue-next'
```

---

### 步骤 2：注册工具元数据

在 `src/lib/tools-data.ts` 中添加工具配置：

```typescript
export const toolsData: ToolsData = {
  // ... 其他工具
  
  timestampConverter: {  // 工具唯一标识（camelCase）
    label: "时间戳转换器",           // 显示名称
    description: "Unix时间戳与日期时间互转", // 简短描述
    badges: ["Timestamp", "Date"],   // 标签（用于搜索和分类）
    category: "Text_Manipulation",   // 分类（见下方分类列表）
    path: "/tools/timestamp-converter", // URL 路径
    icon: "Clock",                   // 图标名称（lucide 图标）
    isNew: true,                     // 可选：是否显示"新"标签
  },
}
```

#### 可用分类

参见上方 [📁 菜单分类（一级菜单）说明](#-菜单分类一级菜单说明) 部分。

---

### 步骤 3：添加路由

在 `src/router/index.ts` 的 `children` 数组中添加路由：

```typescript
{
  path: '/tools/timestamp-converter',
  name: 'timestamp-converter',
  component: () => import('../views/tools/TimestampConverter/index.vue'),
},
```

---

### 步骤 4：添加国际化文本

#### 中文 (`locales/zh.yml`)

```yaml
tools:
  # ... 其他工具
  
  timestamp:
    title: '时间戳转换器'
    description: 'Unix时间戳与日期时间互转'
    input: '输入'
    output: '输出'
    convert: '转换'
    # 添加更多工具特定的翻译键...
```

#### 英文 (`locales/en.yml`)

```yaml
tools:
  # ... 其他工具
  
  timestamp:
    title: 'Timestamp Converter'
    description: 'Convert between Unix timestamp and date/time'
    input: 'Input'
    output: 'Output'
    convert: 'Convert'
    # 添加更多工具特定的翻译键...
```

---

## 🎨 UI 设计规范

### 布局结构

```
┌─────────────────────────────────────────┐
│  BaseCard (p-6)                         │
│  ┌───────────────────────────────────┐  │
│  │ 标题 (h1, text-2xl)               │  │
│  │ 描述 (p, text-sm, text-muted)     │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │ 主内容区 (space-y-6)               │  │
│  │ - 输入控件                        │  │
│  │ - 操作按钮                        │  │
│  │ - 输出展示                        │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │ 底部说明 (border-t, text-xs)      │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 常用样式类

| 用途 | 类名 |
|------|------|
| 最大宽度 | `max-w-4xl` |
| 卡片内边距 | `p-6` |
| 垂直间距 | `space-y-2`, `space-y-4`, `space-y-6` |
| 水平间距 | `gap-2`, `gap-4` |
| 文本颜色（次要） | `text-muted-foreground` |
| 等宽字体 | `font-mono` |
| 边框分隔 | `border-t`, `pt-6` |

---

## 🔧 常用功能模式

### Toast 提示

```typescript
import { useToast } from '@/composables/useToast'

const { toast } = useToast()

// 成功提示
toast({
  title: '操作成功',
  variant: 'success',
  duration: 2000,
})

// 错误提示
toast({
  title: '操作失败',
  description: error.message,
  variant: 'error',
})
```

### 复制到剪贴板

```typescript
function copyToClipboard(text: string) {
  navigator.clipboard.writeText(text)
  toast({
    title: t('common.copied'),
    variant: 'success',
    duration: 2000,
  })
}
```

### 国际化

```typescript
import { useI18n } from 'vue-i18n'

const { t, locale } = useI18n()

// 使用翻译
t('tools.xxx.title')
t('common.copy')

// 检查当前语言
if (locale.value === 'zh') {
  // 中文逻辑
}
```

---

## 📦 添加新依赖

如果工具需要额外的 npm 包：

```bash
pnpm add <package-name>
```

常用工具库：
- `crypto-js` - 加密/哈希
- `uuid` - UUID 生成
- `nanoid` - Nano ID 生成
- `ulid` - ULID 生成
- `dayjs` / `date-fns` - 日期处理

---

## ✅ 检查清单

创建新工具后，请确认以下事项：

- [ ] 视图组件创建完成 (`src/views/tools/<ToolName>/index.vue` 和 `use<ToolName>.ts`)
- [ ] 工具元数据已添加 (`src/lib/tools-data.ts`)
- [ ] 路由已配置 (`src/router/index.ts`)
- [ ] 中文翻译已添加 (`locales/zh.yml`)
- [ ] 英文翻译已添加 (`locales/en.yml`)
- [ ] 页面能正常访问
- [ ] 功能正常工作
- [ ] 复制功能正常
- [ ] 深色模式下显示正常
- [ ] 中英文切换正常
