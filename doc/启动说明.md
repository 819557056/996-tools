# 996-Tools 启动说明

## 快速开始指南

### 1. 环境准备

确保您的系统已安装：
- **Node.js**: 版本 18 或更高
- **包管理器**: npm、pnpm 或 yarn（推荐使用 pnpm）

检查版本：
```bash
node --version  # 应该显示 v18.x.x 或更高
npm --version   # 应该显示 9.x.x 或更高
```

### 2. 安装依赖

在项目根目录下运行：

```bash
# 使用 pnpm (推荐)
pnpm install

# 或使用 npm
npm install

# 或使用 yarn
yarn install
```

### 3. 启动开发服务器

```bash
# 使用 pnpm
pnpm dev

# 或使用 npm
npm run dev

# 或使用 yarn
yarn dev
```

开发服务器将在 `http://localhost:3000` 启动。浏览器会自动打开。

### 4. 构建生产版本

当您准备好部署时：

```bash
# 使用 pnpm
pnpm build

# 或使用 npm
npm run build

# 或使用 yarn
yarn build
```

构建的文件将输出到 `dist` 目录。

### 5. 预览生产构建

在本地预览生产构建：

```bash
# 使用 pnpm
pnpm preview

# 或使用 npm
npm run preview

# 或使用 yarn
yarn preview
```

## 常见问题

### 端口被占用

如果 3000 端口被占用，您可以：
1. 修改 `vite.config.ts` 中的端口号
2. 或在启动时指定端口：`pnpm dev --port 3001`

### 依赖安装失败

1. 清除缓存：
   ```bash
   # npm
   npm cache clean --force
   
   # pnpm
   pnpm store prune
   ```

2. 删除 `node_modules` 和锁文件后重新安装：
   ```bash
   rm -rf node_modules
   rm pnpm-lock.yaml  # 或 package-lock.json
   pnpm install
   ```

### 类型错误

运行类型检查：
```bash
pnpm vue-tsc --noEmit
```

## 开发建议

### IDE 设置

推荐使用 **Visual Studio Code** 并安装以下扩展：
- Vue Language Features (Volar)
- TypeScript Vue Plugin (Volar)
- Tailwind CSS IntelliSense
- ESLint

### 代码格式化

项目配置了 ESLint。您可以在 VSCode 设置中启用保存时自动格式化：

```json
{
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

## 项目特性

### 主题切换
- 支持明亮/暗黑主题
- 自动保存主题偏好
- 点击右上角图标切换

### 工具使用
- 所有工具都在浏览器本地运行
- 不会向服务器发送任何数据
- 确保隐私和安全

### 添加新工具

1. 在 `src/lib/tools-data.ts` 添加工具配置：
```typescript
newTool: {
  label: "新工具",
  description: "工具描述",
  badges: ["标签1", "标签2"],
  category: "分类",
  path: "/tools/new-tool",
  icon: "Icon",
}
```

2. 在 `src/views/tools/` 创建组件：
```vue
<template>
  <!-- 工具界面 -->
</template>

<script setup lang="ts">
// 工具逻辑
</script>
```

3. 在 `src/router/index.ts` 添加路由：
```typescript
{
  path: '/tools/new-tool',
  name: 'new-tool',
  component: () => import('../views/tools/NewToolView.vue'),
}
```

## 部署

### 静态托管平台

项目可以部署到任何静态托管平台：

- **Vercel**: 
  ```bash
  vercel
  ```

- **Netlify**: 将 `dist` 目录拖放到 Netlify 面板

- **GitHub Pages**: 
  1. 修改 `vite.config.ts` 添加 `base: '/repo-name/'`
  2. 构建项目
  3. 将 `dist` 目录推送到 `gh-pages` 分支

### Nginx 配置

如果使用 Nginx，配置示例：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## 技术支持

如有问题，请查看：
- [Vue 3 文档](https://vuejs.org/)
- [Vite 文档](https://vitejs.dev/)
- [Tailwind CSS 文档](https://tailwindcss.com/)

---

祝您使用愉快！ 🎉

